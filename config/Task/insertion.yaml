name: insertion

peg:
  pos:
    ranges: [[0.1, 0.2], [0.4, 0.6], [0.05, 0.05]]
    value: # to be changed from outside
  quat: [1, 0, 0, 0]

soc: # socket
  pos:
    ranges: [[-0.2, -0.1], [0.4, 0.6], [0.05, 0.05]]
    value: # to be changed from outside
  quat: [1, 0, 0, 0]
